import { ExecutionResult } from "graphql";
export declare type ExecutionLivePatchResult<PatchPayload = unknown> = {
    errors?: ExecutionResult["errors"];
    data?: ExecutionResult["data"];
    patch?: PatchPayload;
    revision?: number;
    extensions?: ExecutionResult["extensions"];
};
