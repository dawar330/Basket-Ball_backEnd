import type { ExecutionResult } from "graphql";
import type { LiveExecutionResult } from "@n1ru4l/graphql-live-query";
import { GeneratePatchFunction } from "./createLiveQueryPatchGenerator.js";
declare type MaybePromise<T> = T | Promise<T>;
declare type LiveQueryDeflatorExecutionResult = MaybePromise<AsyncIterableIterator<ExecutionResult | LiveExecutionResult> | ExecutionResult>;
/**
 *  after-ware for wrapping execute in order to generate live query patches.
 */
export declare const createApplyLiveQueryPatchGenerator: <PatchPayload = unknown>(generatePatch: GeneratePatchFunction<PatchPayload>) => (executionResult: LiveQueryDeflatorExecutionResult) => LiveQueryDeflatorExecutionResult;
export {};
